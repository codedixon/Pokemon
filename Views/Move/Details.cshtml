@model Poke.Models.Moves

@{
    ViewData["Title"] = "View";
    var move = ViewData["move"] as Moves;
}

<h1>View</h1>

<div>
    <h4>Moves</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @move.id
        </dt>
        <dd class = "col-sm-10">
            @move.name
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
